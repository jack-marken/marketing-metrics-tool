

# Installation

Install Node v24.19.0 (references .nvmrc):

```bash
$ nvm i
```

Install NPM packages:

```bash
$ npm i
```

# Usage instructions

Start the server:

```bash
$ npm run server
```

In a new terminal, run the demo code:

```bash
$ node api_demo.js
```